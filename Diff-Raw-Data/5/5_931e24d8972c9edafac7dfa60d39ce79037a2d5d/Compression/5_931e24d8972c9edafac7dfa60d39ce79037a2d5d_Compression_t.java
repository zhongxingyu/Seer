 package gov.nih.nci.iso21090;
 
 /**
  * Represents the iso enum.
 * @author lpower
  */
 public enum Compression {

     /** deflate. */
     DF,
     /** gzip. */
     GZ,
     /** zlib. */
     ZL,
     /** compress. */
     Z,
     /** bzip. */
     BZ,
     /** z7. */
     Z7;
 }
